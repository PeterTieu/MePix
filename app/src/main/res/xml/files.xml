<?xml version="1.0" encoding="utf-8"?>

<paths>
    <files-path name="pix_photos" path="."/>    //means: Map the root path of the private storage as: crime_photos
    //Next step: Hook up files.xml (this file) to the FileProvider
    //by adding a meta-data tag in the AndroidManifest.xml

</paths>